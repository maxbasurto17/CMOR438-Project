[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rice_ml"
version = "0.1.0"
description = "Machine learning project for class."
readme = "README.md"

dependencies = [
    "numpy",
]